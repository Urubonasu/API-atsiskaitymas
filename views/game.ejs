<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="../game.css">

</head>
<body>
    <div class="game-details">
        <h1 class="title"><%= game.name %></h1>
        <h2 class="review"><%= game.review %></h2>
        <h2 class="genre">Genre: <%= game.genre %></h2>
        <h2 class="pegi">Rated PEGI:<%= game.pegi %></h2>
        <h2 class="platform">Platform(s): <%= game.platform %></h2>
        <button type="button" class="btn btn-danger delete" data-del="<%= game._id %>" >Delete</button>
    </div>
    <script>
        const istrinti = document.querySelector('button.delete')

        istrinti.addEventListener('click', (e) => {
            const taskas = `/${istrinti.dataset.del}`

            fetch(taskas, {
                method:'DELETE'
            })
            .then((response) => response.json())
            .then((data) => window.location.href = data.redirect)
            .catch(err => console.log(err))
        })
    </script>


</body>
</html>